<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.coronavirus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="ExtraText">
        <activity android:name=".MostrarSuspeito" />
        <activity android:name=".MostrarPaciente" />
        <activity android:name=".DisplayVerEstatistica" />
        <activity android:name=".DisplayInserirPaciente" />
        <activity android:name=".DisplayInserirSuspeito" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".ContentProvider"
            android:authorities="pt.example.coronavirus"
            android:exported="false" />
    </application>

</manifest>